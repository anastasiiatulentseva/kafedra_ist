- provide(:title, t('new_workbook'))
%h1.page-header
  = t('new_workbook')
.row
  .col-md-10.col-md-offset-1
    = render partial: 'workbooks/workbook_form',
      locals: { object: @workbook,
        url: workbooks_path,
        http_method: :post,
        subjects: @subjects }
